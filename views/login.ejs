<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login - Work Permits</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="login-page">
  <main class="container auth-layout auth-shell">
    <section class="login-aside" aria-hidden="true">
      <div class="login-aside-overlay">
        <p class="aside-kicker">SACHEM EHS&S</p>
        <h1>Work permit control center</h1>
        <p>One secure place to create, review, and sign permits across teams.</p>
        <ul>
          <li>Permit lifecycle tracking</li>
          <li>Live sign-off accountability</li>
          <li>Audit-ready activity logs</li>
        </ul>
      </div>
    </section>

    <section class="card login-card">
      <div class="login-glow" aria-hidden="true"></div>
      <div class="login-header">
        <div class="login-logo">
          <img src="/img/sachem.gif" alt="Sachem" />
        </div>
        <div>
          <p class="login-label">SACHEM Work Permits</p>
          <p class="login-subtitle">Secure access for permit teams</p>
        </div>
      </div>

      <% if (error) { %><p class="error login-error"><%= error %></p><% } %>

      <form method="post" action="/login" class="form-stack login-form" id="loginForm">
        <label>
          <span>Username</span>
          <input id="username" name="username" required autocomplete="username" placeholder="Enter your username" />
        </label>
        <label>
          <span>Password</span>
          <div class="password-wrap">
            <input id="password" type="password" name="password" required autocomplete="current-password" placeholder="Enter your password" />
            <button type="button" class="password-toggle" id="togglePassword" aria-label="Show password" aria-pressed="false">Show</button>
          </div>
          <small id="capsHint" class="caps-hint" hidden>Caps Lock is on</small>
        </label>
        <button type="submit" class="button primary login-action">Sign In</button>
      </form>

      <div class="login-meta">
        <span class="status-dot" aria-hidden="true"></span>
        <span>Secure connection active</span>
      </div>
      <p class="login-footnote">Safety first. Access restricted to authorized personnel.</p>
    </section>
  </main>

  <script>
    (function () {
      const username = document.getElementById('username');
      const password = document.getElementById('password');
      const toggle = document.getElementById('togglePassword');
      const capsHint = document.getElementById('capsHint');

      if (username) username.focus();

      if (toggle && password) {
        toggle.addEventListener('click', function () {
          const isHidden = password.type === 'password';
          password.type = isHidden ? 'text' : 'password';
          toggle.textContent = isHidden ? 'Hide' : 'Show';
          toggle.setAttribute('aria-pressed', isHidden ? 'true' : 'false');
          toggle.setAttribute('aria-label', isHidden ? 'Hide password' : 'Show password');
        });
      }

      function handleCaps(event) {
        const on = !!(event.getModifierState && event.getModifierState('CapsLock'));
        if (capsHint) capsHint.hidden = !on;
      }

      if (password) {
        password.addEventListener('keydown', handleCaps);
        password.addEventListener('keyup', handleCaps);
        password.addEventListener('blur', function () { if (capsHint) capsHint.hidden = true; });
      }
    })();
  </script>
</body>
</html>
